[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "cyberdeck-rs"
version = "0.1.0"
description = "PyO3 fast path for the cyberdeck orchestrator"
requires-python = ">=3.8"
